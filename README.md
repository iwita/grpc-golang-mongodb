# Application using gRPC,Golang and mongoDB.

Sample CRUD (Create, Read, Update, Delete) client-server application using:
 - gRPC as the API
 - Golang as the implementation
 - mongoDB as the database
